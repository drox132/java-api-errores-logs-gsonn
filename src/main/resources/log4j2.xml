<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <!-- Define un Appender para escribir los logs en un archivo -->
    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %c{1} - %msg%n"/>
        </Console>

        <File name="LogFile" fileName="logs/app.log" append="true">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %c{1} - %msg%n"/>

            <!--opcional para configurar si el tamaño del archivo del log pasa de los 10 mb-->
            <Policies>
                <SizeBasedTriggeringPolicy size="10MB"/>
                <TimeBasedTriggeringPolicy interval="1" modulate="true"/>
            </Policies>

        </File>
    </Appenders>

    <!-- Configura el Logger root para capturar logs de toda la aplicación -->
    <Loggers>
        <Root level="debug">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="LogFile"/>
        </Root>
    </Loggers>
</Configuration>
